#include<stdio.h>
arr

int GetSet(int *a){
   printf("enter number of element:");
   scanf("%d", a);
   printf("Enter element:");
   for (int i = 0; i < *a; i++){
        printf("%d", data[100]);
   }

}
int main(){
    int *data,num;
    data = GetSet(&num);

}